# window_ext

FlClash desktop lifecycle extensions that are intentionally kept outside
`window_manager`: Windows second-instance activation and corner preference,
plus the macOS application termination callback.
